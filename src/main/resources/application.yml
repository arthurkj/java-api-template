spring:
  application:
    name: template
  datasource:
    url: jdbc:postgresql://localhost:5432/db
    username: user
    password: password
  jpa:
    show-sql: true

logging:
  level:
    br.com.akj: DEBUG

### ACTUATOR ###
# define para sempre exibir os detalhes no endpoint health check
management:
  endpoint.health.show-details: always
# define quais recursos serão expostos
  endpoints.web.exposure.include: health,info,metrics

### LIQUIBASE ###
liquibase:
  enabled: true
  liquibase-schema: hello-user
